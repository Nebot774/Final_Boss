import 'package:flutter/material.dart';

class InformationSpirit extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Inserta aquí el contenido para la página InformationSpirit.
    return Center(child: Text('Información sobre Spirit Rover'));
  }
}